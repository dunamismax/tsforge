# scratch/

Playground for one-off scripts, experiments, and work-in-progress.

Anything durable should move into the workspace proper:

- standalone utilities -> `scripts/`
- shared logic -> `packages/`
- product surfaces -> `apps/`
- persistent contracts -> `packages/contracts/`

No quality bar enforced here. This is still the messy workbench.
