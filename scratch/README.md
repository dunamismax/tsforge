# scratch/

Playground for one-off scripts, experiments, and work-in-progress.

Scripts here are temporary. If something proves useful and reusable, move the implementation into `pyforge/`, add a stable entrypoint in `tools/` if needed, and cover the behavior with tests.

No quality bar enforced here — this is the messy workbench.
