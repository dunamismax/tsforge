# LUTs

Shared LUT assets for video-related scripts live here.

## Current layout

- `dji-official/` for vendor LUTs
- `custom/` for project- or workflow-specific LUTs

Scripts should treat this as a convenience library, not a hard requirement. Users can
always pass a custom LUT path directly.
